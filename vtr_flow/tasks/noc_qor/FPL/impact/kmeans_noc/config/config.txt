##############################################
# Configuration file for running experiments
##############################################

# Path to directory of circuits to use
circuits_dir=benchmarks/noc/NoCImpact/kmeans/

# Path to directory of architectures to use
archs_dir=arch/noc/FPL

# Path to directory of NoC Traffic Patterns to use
noc_traffics_dir=benchmarks/noc/NoCImpact/kmeans/

# Traffic flow files are added per circuit.
# By default, the task performs a cartesian product between circuits and traffic flow files.
# Since each circuit is compatible with specific traffic flow files, we need to specify
# traffic flow files for each circuit separately.
noc_traffic_list_type=per_circuit

# Add circuits and traffic flows to list to sweep
circuit_list_add=kmeans_noc.blif
noc_traffic_list_add=kmeans_noc.flows

# Add architectures to list to sweep
arch_list_add=stratixiv_arch.timing_with_a_embedded_4x4_mesh_noc_topology_512.xml
arch_list_add=stratixiv_arch.timing_with_a_embedded_4x4_mesh_noc_topology_512_all.xml
arch_list_add=stratixiv_arch.timing_with_a_embedded_4x4_mesh_noc_topology_512_external.xml

# Parse info and how to parse
parse_file=vpr_noc.txt

# How to parse QoR info
qor_parse_file=qor_noc_spec.txt

# Pass requirements
#pass_requirements_file=

# Script parameters
script_params_common =-starting_stage vpr --place --route --net_file /home/soheil/kmeans_noc.net --noc on --noc_routing_algorithm odd_even_routing --route_chan_width 300 --device "EP4SE820" -read_vpr_constraints tasks/noc_qor/FPL/impact/kmeans_noc/kmeans_noc_constraint.xml --noc_centroid_weight 0.2