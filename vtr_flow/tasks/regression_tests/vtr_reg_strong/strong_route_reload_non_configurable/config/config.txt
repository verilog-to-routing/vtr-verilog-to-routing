##############################################
# Configuration file for running experiments
##############################################

# Path to directory of circuits to use
circuits_dir=benchmarks/blif/wiremap6

# Path to directory of architectures to use
archs_dir=arch/custom_grid

# Add circuits to list to sweep
circuit_list_add=alu4.pre-vpr.blif

# Add architectures to list to sweep
arch_list_add=shorted_flyover_wires.xml

# Parse info and how to parse
parse_file=vpr_fixed_chan_width.txt
parse_file=vpr_parse_second_file.txt

# How to parse QoR info
qor_parse_file=qor_standard.txt

# Pass requirements
pass_requirements_file=pass_requirements_analysis_only.txt

# Route once, then reload the route for analysis. Writing and reloading the
# RR graph triggers the flow's second VPR invocation.
script_params=-starting_stage vpr -check_route -verify_rr_graph --route_chan_width 60
