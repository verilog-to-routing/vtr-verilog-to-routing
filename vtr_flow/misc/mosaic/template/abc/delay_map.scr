# delay_map.scr - pass2 fracturable lut map (after gia opt) or standalone
# invoke: abc -luts <cost> -script <this file>
# LF-only ASCII. classic only.

strash
ifraig
dc2
dch -f
dc2
dch -f
dc2
dch -f
if
mfs2
mfs2
lutpack
