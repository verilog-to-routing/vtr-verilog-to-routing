// adder stub for vtr_classic -lib resolution; WIDTH comes from the arch scan.
(* blackbox *)
module @ADDER_MODEL@ #(
    parameter WIDTH = @ADDER_WIDTH@
) (
    input [WIDTH-1:0] a,
    input [WIDTH-1:0] b,
    input cin,
    output cout,
    output [WIDTH-1:0] sumout
);
endmodule
