if (QT_MIGRATION_FAST_BUILD)
    message("WARNING!!! QT_MIGRATION_FAST_BUILD is ON: skip fasm, route_diag, vqm2blif build")
else()
    add_subdirectory(fasm)
    add_subdirectory(route_diag)
    add_subdirectory(vqm2blif)
endif()