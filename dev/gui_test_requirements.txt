# Python dependencies used ONLY by VTR's GUI tests (not needed to build or run
# VTR itself):
#   - numpy / scikit-image / Pillow : GUI Layer-5 visual regression (SSIM compare)
#   - gcovr                         : GUI coverage report / regression gate
#
# Pinned to Ubuntu 24.04's distro versions (the CI runner OS). These have
# Python 3.12 wheels on PyPI, so they install without a source build.
numpy==1.26.4
scikit-image==0.22.0
Pillow==12.2.0
gcovr==8.6
